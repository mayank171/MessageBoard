<!DOCTYPE html>
<html>
  <head>
    <title>Void</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

 
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    </head>

  <body>

  <div class="navbar navbar-default">
     <div class="container">
        <div class="navbar-header">
           <%= link_to "Void 0.0", root_path, class: "navbar-brand" %>
        </div>
        <ul class="nav navbar-nav">
            <li><%= link_to "Sign Up", new_user_registration_path, class: "link-primary" %></li>
            <% if user_signed_in? %>
               <li> <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "link-primary"  %></li>
            <% else %>
               <li><%= link_to "Log In", new_user_session_path, class: "link-primary" %></li>
            <% end %>   
        </ul>
        <% if user_signed_in? %>
            <p><%= link_to "New Message",new_message_path,class: "navbar-right navbar-text navbar-link btn btn-primary" %></p>
        <% end %>
     </div>
  </div>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>


    <div class="container">
       <%= yield %>
    </div>


    
  </body>
</html>
